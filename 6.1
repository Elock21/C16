#include <iostream>
using namespace std;
void bubblesort(int v[],int n)
{
int i,j,aux;
for(i=0;i<n-1;i++)
for(j=i+1;j<n;j++)
if(v[i]>v[j]) {
aux=v[i];
v[i]=v[j];
v[j]=aux;
}
}
void insertie(int v[],int n)
{
int i,j,aux;
for(i=1;i<n;i++)
{
j=i;
while( (j>0) && (v[j-1]>v[j]) )
{
aux=v[j];
v[j]=v[j-1];
v[j-1]=aux;
j--;
}
}
}
void selectie(int v[],int n)
{
int poz_min,i,j,aux;
for(i=0;i<n-1;i++)
{
poz_min=i;
for(j=i+1;j<n;j++)
if(v[j]<v[poz_min])
poz_min=j;
if(poz_min!=i)
{aux=v[i];
v[i]=v[poz_min];
v[poz_min]=aux;
}
}
}
void quicksort(int v[],int st,int dr)
{
int i,j,m,aux,d;
if(st < dr)
{
m=(st+dr)/2;
aux=v[st];
v[st]=v[m];
v[m]=aux;
int i=st,j=dr,d=0;
while(i<j)
{
if(v[i]>v[j])
{
aux=v[i];
v[i]=v[j];
v[j]=aux;
d=1-d;
}
i=i+d;
j=j-(1-d);
}
quicksort(v,st,i-1);
quicksort(v,i+1,dr);
}
}
int main()
{
int n,i,var;
cout<<"n=";
cin>>n;
int v[n];
cout<<"Introduceti "<<n<<" valori in vector: ";
for(i=0;i<n;i++)
cin>>v[i];
cout<<"Alegeti metoda de sortare"<<endl;
cout<<"1) Bubble sort"<<endl;
cout<<"2) Insertie"<<endl;
cout<<"3) Selectie"<<endl;
cout<<"4) Quicksort"<<endl;
cin>>var;
while(var<1 || var>4)
{
cin>>var;
}
switch(var)
{
case 1: bubblesort(v,n);break;
case 2: insertie(v,n);break;
case 3: selectie(v,n);break;
case 4: quicksort(v,0,n);break;
}
cout<<"Vectorul sortat: ";
for(i=0;i<n;i++)
cout<<v[i]<<' ';
}
